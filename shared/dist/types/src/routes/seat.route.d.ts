import IRoute from '@interfaces/route.interface';
export default class SeatRoute implements IRoute {
    path?: string;
    router: any;
    private readonly seatController;
    constructor();
    private initializeRoutes;
}

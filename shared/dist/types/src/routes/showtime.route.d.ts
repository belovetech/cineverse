import IRoute from '@interfaces/route.interface';
export default class ShowtimeRoute implements IRoute {
    path?: string;
    router: any;
    private readonly showtimeController;
    constructor();
    private initializeRoutes;
}

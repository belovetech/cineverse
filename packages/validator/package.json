{
  "name": "@cineverse/validator",
  "version": "1.0.0",
  "description": "Utility validator for cineverse microservices",
  "packageManager": "yarn@3.6.1",
  "main": "./dist/src/validator.js",
  "license": "MIT",
  "scripts": {
    "start": "yarn run build && cross-env NODE_ENV=development nodemon dist/src/validator.js",
    "build": "tsc && npx tsc-alias",
    "lint": "eslint --ignore-path .gitignore --ext .ts src/",
    "lint:fix": "yarn run lint --fix",
    "clean": "yarn cache clean"
  },
  "devDependencies": {
    "@types/node": "^20.4.4",
    "cross-env": "^7.0.3",
    "tsconfig-paths": "^4.2.0"
  }
}

{
  "name": "@cineverse/exceptions",
  "version": "1.0.0",
  "description": "Utility exceptions for cineverse microservices",
  "packageManager": "yarn@3.6.1",
  "main": "./dist/src/index.js",
  "license": "MIT",
  "scripts": {
    "start": "yarn run build && cross-env NODE_ENV=development nodemon dist/src/index.js",
    "build": "tsc && npx tsc-alias",
    "lint": "eslint --ignore-path .gitignore --ext .ts src/",
    "lint:fix": "yarn run lint --fix"
  },
  "devDependencies": {
    "@types/node": "^20.4.4",
    "cross-env": "^7.0.3",
    "tsconfig-paths": "^4.2.0"
  }
}

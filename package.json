{
  "name": "cineverse",
  "version": "1.0.0",
  "author": "Abeeb Raheem",
  "license": "ISC",
  "private": true,
  "scripts": {
    "start": "pnpm --stream -r run dev",
    "build": "pnpm --stream -r run build",
    "clean": "pnpm -r run clean",
    "auth":  "pnpm run dev --filter auth-service"
  }
}
